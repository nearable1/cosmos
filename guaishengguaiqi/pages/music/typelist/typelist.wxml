<image src="{{imgUrls}}" style="width:100%;height:500rpx"></image> 

<view class="myaudio" wx:for="{{list}}"  wx:key="{{index}}" wx:for="{{list}}" id="{{index}}" bindtap="click">{{item.name}}</view>

<!--<audio wx:key="{{index}}" wx:for="{{list}}" poster="{{item.post}}" name="{{item.name}}" author="" src="{{item.path}}" id="myAudio" controls loop></audio>

