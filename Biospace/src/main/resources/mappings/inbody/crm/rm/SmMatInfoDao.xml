<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inbody.crm.rm.dao.SmMatInfoDao">

    <sql id="smMatInfoColumns">
        a.id AS "id",
        a.proc_ins_id AS "procInsId",
        a.create_by AS "createBy.id",
        a.create_date AS "createDate",
        a.update_by AS "updateBy.id",
        a.update_date AS "updateDate",
        a.del_flag AS "delFlag",
        a.material_no AS "materialNo",
        a.material_name AS "materialName",
        a.unit AS "unit",
        a.if_sn AS "ifSn",
        a.model AS "model",
        a.if_virtual_sn AS "ifVirtualSn",
        a.new_remarks AS "newRemarks",
        a.material_type AS "materialType",
        a.old_material_no AS "oldMaterialNo",
        a.safety_stock AS "safetyStock"
    </sql>

    <sql id="smMatInfoJoins">
    </sql>

    <select id="get" resultType="com.inbody.crm.rm.entity.SmMatInfo">
        SELECT 
            <include refid="smMatInfoColumns"/>
        FROM sm_mat_info a
        <include refid="smMatInfoJoins"/>
        WHERE a.id = #{id}
    </select>

    <select id="findList" resultType="com.inbody.crm.rm.entity.SmMatInfo">
        SELECT 
            <include refid="smMatInfoColumns"/>
        FROM sm_mat_info a
        <include refid="smMatInfoJoins"/>
        <where>
            a.del_flag = #{DEL_FLAG_NORMAL}
        </where>
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
                ORDER BY a.update_date DESC
            </otherwise>
        </choose>
    </select>

    <select id="findAllList" resultType="com.inbody.crm.rm.entity.SmMatInfo">
        SELECT 
            <include refid="smMatInfoColumns"/>
        FROM sm_mat_info a
        <include refid="smMatInfoJoins"/>
        <where>
            a.del_flag = #{DEL_FLAG_NORMAL}
        </where>        
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
                ORDER BY a.update_date DESC
            </otherwise>
        </choose>
    </select>

    <insert id="insert">
        INSERT INTO sm_mat_info(
            id,
            proc_ins_id,
            create_by,
            create_date,
            update_by,
            update_date,
            del_flag,
            material_no,
            material_name,
            unit,
            if_sn,
            model,
            if_virtual_sn,
            new_remarks,
            material_type,
            old_material_no,
            safety_stock
        ) VALUES (
            #{id},
            #{procInsId},
            #{createBy.id},
            #{createDate},
            #{updateBy.id},
            #{updateDate},
            #{delFlag},
            #{materialNo},
            #{materialName},
            #{unit},
            #{ifSn},
            #{model},
            #{ifVirtualSn},
            #{newRemarks},
            #{materialType},
            #{oldMaterialNo},
            #{safetyStock}
        )
    </insert>

    <update id="update">
        UPDATE sm_mat_info SET     
            proc_ins_id = #{procInsId},
            update_by = #{updateBy.id},
            update_date = #{updateDate},
            material_no = #{materialNo},
            material_name = #{materialName},
            unit = #{unit},
            if_sn = #{ifSn},
            model = #{model},
            if_virtual_sn = #{ifVirtualSn},
            new_remarks = #{newRemarks},
            material_type = #{materialType},
            old_material_no = #{oldMaterialNo},
            safety_stock = #{safetyStock}
        WHERE id = #{id}
    </update>

    <update id="delete">
        UPDATE sm_mat_info SET 
            del_flag = #{DEL_FLAG_DELETE}
        WHERE id = #{id}
    </update>

    <!-- 报价单明细物料信息 -->
    <select id="getMaterialInfoByNo" resultType="com.inbody.crm.rm.entity.SmMatInfo">
        SELECT 
            <include refid="smMatInfoColumns"/>
        FROM sm_mat_info a
        WHERE a.material_no = #{materialNo}
        AND a.del_flag = '0'
    </select>
</mapper>