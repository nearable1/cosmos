<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inbody.crm.rm.dao.ImInvoiceDao">
    
	<sql id="imInvoiceColumns">
		a.id AS "id",
		a.proc_ins_id AS "procInsId",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag",
		a.order_no AS "orderNo",
		a.ticket_method AS "ticketMethod",
		a.invoice_title AS "invoiceTitle",
		a.taxpayer_ident_no AS "taxpayerIdentNo",
		a.deposit_bank AS "depositBank",
		a.bank_account AS "bankAccount",
		a.invoice_address AS "invoiceAddress",
		a.telephone AS "telephone",
		a.new_remarks AS "newRemarks",
		a.recipients AS "recipients",
		a.rep_telephone AS "repTelephone",
		a.workflow_status AS "workflowStatus",
		a.address AS "address"
	</sql>
	
	<sql id="imInvoiceJoins">
	</sql>
    
	<select id="get" resultType="com.inbody.crm.rm.entity.ImInvoice">
		SELECT 
			<include refid="imInvoiceColumns"/>
		FROM im_invoice a
		<include refid="imInvoiceJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="com.inbody.crm.rm.entity.ImInvoice">
		SELECT 
			<include refid="imInvoiceColumns"/>
		FROM im_invoice a
		<include refid="imInvoiceJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="com.inbody.crm.rm.entity.ImInvoice">
		SELECT 
			<include refid="imInvoiceColumns"/>
		FROM im_invoice a
		<include refid="imInvoiceJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO im_invoice(
			id,
			proc_ins_id,
			create_by,
			create_date,
			update_by,
			update_date,
			del_flag,
			order_no,
			ticket_method,
			invoice_title,
			taxpayer_ident_no,
			deposit_bank,
			bank_account,
			invoice_address,
			telephone,
			new_remarks,
			recipients,
			rep_telephone,
			workflow_status,
			address
		) VALUES (
			#{id},
			#{procInsId},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{delFlag},
			#{orderNo},
			#{ticketMethod},
			#{invoiceTitle},
			#{taxpayerIdentNo},
			#{depositBank},
			#{bankAccount},
			#{invoiceAddress},
			#{telephone},
			#{newRemarks},
			#{recipients},
			#{repTelephone},
			#{workflowStatus},
			#{address}
		)
	</insert>
	
	<update id="update">
		UPDATE im_invoice SET 	
			proc_ins_id = #{procInsId},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			order_no = #{orderNo},
			ticket_method = #{ticketMethod},
			invoice_title = #{invoiceTitle},
			taxpayer_ident_no = #{taxpayerIdentNo},
			deposit_bank = #{depositBank},
			bank_account = #{bankAccount},
			invoice_address = #{invoiceAddress},
			telephone = #{telephone},
			new_remarks = #{newRemarks},
			recipients = #{recipients},
			rep_telephone = #{repTelephone},
			workflow_status = #{workflowStatus},
			address = #{address}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE im_invoice SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>

    <!-- 根据id取得发票信息 -->
    <select id="getInvoiceById" resultType="com.inbody.crm.rm.entity.ImInvoice">
        SELECT 
            <include refid="imInvoiceColumns"/>,
            b.invoice_type AS "invoiceType"
        FROM im_invoice a
        INNER JOIN im_invoice_dtl b on b.app_id = a.id
        WHERE a.del_flag = '0'
        AND a.id = #{id}
        GROUP BY a.id
    </select>

    <!-- 根据报价单编号取得发票信息 -->
    <select id="getInvoiceByQuotaNo" resultType="com.inbody.crm.rm.entity.ImInvoice">
        SELECT 
            <include refid="imInvoiceColumns"/>
        FROM im_invoice a
        WHERE a.del_flag = '0'
        AND a.order_no = #{orderNo}
        ORDER BY a.create_date ASC
    </select>

    <!-- 根据报价单编号取得创建时间最晚的发票信息 -->
    <select id="getLastInvoiceByQuotaNo" resultType="com.inbody.crm.rm.entity.ImInvoice">
        SELECT 
            <include refid="imInvoiceColumns"/>,
            b.invoice_type AS "invoiceType"
        FROM im_invoice a
        INNER JOIN im_invoice_dtl b on b.app_id = a.id
        WHERE a.del_flag = '0'
        AND a.order_no = #{orderNo}
        ORDER BY a.create_date DESC
        LIMIT 1
    </select>
    
    <!-- 根据报价单id取得未完成发票申请数 -->
    <select id="getUnfinishedApplyInvoice" resultType="com.inbody.crm.rm.entity.ImInvoice">
        SELECT 
            <include refid="imInvoiceColumns"/>
        FROM im_invoice a
        INNER JOIN rm_quotation b ON b.quotation_no = a.order_no
        <where>
            a.del_flag = '0'
            AND b.del_flag = '0'
            AND b.id = #{quotaId}
            AND a.workflow_status &lt;&gt; '50'
        </where>
    </select>
</mapper>