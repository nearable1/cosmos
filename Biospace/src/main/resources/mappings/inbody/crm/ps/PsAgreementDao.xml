<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inbody.crm.ps.dao.PsAgreementDao">
    
	<sql id="PsAgreementColumns">
		a.id AS "id",
		a.proc_ins_id AS "procInsId",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag",
		a.agreement_paties_id AS "agreementPatiesId",
		a.validity_date_from AS "validityDateFrom",
		a.validity_date_to AS "validityDateTo",
		a.new_remarks AS "newRemarks"
	</sql>
	
	<sql id="PsAgreementJoins">
	</sql>
	
	<select id="listProtocols" resultType="com.inbody.crm.common.entity.SelectEntity">
		SELECT a.agreement_paties_id AS "value",i.customer_ch_name AS "label"
		FROM cm_agreement a
		left join cm_customer_info i on a.agreement_paties_id = i.customer_id
		WHERE i.customer_ch_name is not null
		<if test="customerType != null and customerType != '' and customerType != '-'">
			and i.customer_type = #{customerType}
		</if>
		GROUP BY a.agreement_paties_id,i.customer_ch_name
	</select>
	
	<select id="listStartDays" resultType="com.inbody.crm.common.entity.SelectEntity">
		SELECT id AS "value",DATE_FORMAT(a.validity_date_from,"%Y-%m-%d") AS "label"
		FROM cm_agreement a
		WHERE a.agreement_paties_id = #{id}
	</select>
    
	<select id="get" resultType="PsAgreement">
		SELECT 
			<include refid="PsAgreementColumns"/>
		FROM cm_agreement a
		<include refid="PsAgreementJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="getCustomerName" resultType="String">
		SELECT 
			customer_ch_name
		FROM cm_customer_info
		WHERE customer_id = #{id}
	</select>
	
	<select id="findList" resultType="PsAgreement">
		SELECT 
			<include refid="PsAgreementColumns"/>
		FROM cm_agreement a
		<include refid="PsAgreementJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="PsAgreement">
		SELECT 
			<include refid="PsAgreementColumns"/>
		FROM cm_agreement a
		<include refid="PsAgreementJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cm_agreement(
			id,
			proc_ins_id,
			create_by,
			create_date,
			update_by,
			update_date,
			del_flag,
			agreement_id,
			agreement_paties_id,
			validity_date_from,
			validity_date_to,
			new_remarks
		) VALUES (
			#{id},
			#{procInsId},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{delFlag},
			#{agreementId},
			#{agreementPatiesId},
			#{validityDateFrom},
			#{validityDateTo},
			#{newRemarks}
		)
	</insert>
	
	<update id="update">
		UPDATE cm_agreement SET 	
			proc_ins_id = #{procInsId},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			agreement_id = #{agreementId},
			agreement_paties_id = #{agreementPatiesId},
			validity_date_from = #{validityDateFrom},
			validity_date_to = #{validityDateTo},
			new_remarks = #{newRemarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE cm_agreement SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>